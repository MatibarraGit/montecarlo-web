import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['d67a-200-122-69-93.ngrok-free.app'],
  output: "standalone",
};

export default nextConfig;
